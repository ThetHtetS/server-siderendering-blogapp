mixin commentCard(review)
  .comment__card
    .reviews__avatar
      img.reviews__avatar-img(src = review.user.photo, alt=`${review.user.name}`)
      h6.reviews__user= review.user.name
    p.reviews__text= review.comment